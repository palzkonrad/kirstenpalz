# === Nur die Website gehört ins Repo — alles andere ausschließen ===

# Fremd-Projekt: n8n Mail-Automatisierung (enthält E-Mail-Adressen — NICHT veröffentlichen!)
workflow*.json

# Build-/Scrape-Einmalwerkzeuge (gehören nicht zur ausgelieferten Seite)
*.py
__pycache__/
scrape_results.json
extract_log.txt
*.log

# Interne Build-/Test-Artefakte
check_*.js
DESIGN_NOTES.md

# Quell-/Konzeptmaterial
*.docx

# Node-Abhängigkeiten (Build-Werkzeuge, z. B. tools/optimize-images.mjs)
node_modules/

# Heruntergeladene Original-Bilder (Zwischenprodukt von tools/optimize-images.mjs)
tools/_downloads/

# System / Editor
.DS_Store
.antigravity/
*.tmp
